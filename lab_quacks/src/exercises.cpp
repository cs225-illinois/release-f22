/**
 * @file exercies.cpp
 * This file is where you will implement the recursion exercise.
 */

#include "exercises.h"

/**
 * Given a non-negative int n, return the sum of its digits recursively (no
 * loops).
 *
 * @note Note that mod (%) by 10 yields the rightmost digit (126 % 10 is 6),
 *  while divide (/) by 10 removes the rightmost digit (126 / 10 is 12).
 *
 * Examples:
 * - `sumDigits(126)` == 9
 * - `sumDigits(49)` == 13
 * - `sumDigits(12)` == 3
 *
 * @param n The number to sum the digits of
 * @return  The sum of its digits
 */
int RecursionExercises::sumDigits(int n)
{

    return -1;
}

/**
 * Compute the total number of blocks in a triangle of blocks.
 *
 * In a triangle of blocks, the topmost row has 1 block, the next row down has
 * 2 blocks, the next row has 3 blocks, and so on.
 *
 * ```
 *        *        1 block
 *      *   *      2 blocks
 *    *   *   *    3 blocks
 *  *   *   *   *  4 blocks
 * ............... n blocks
 * ```
 *
 * @param rows The number of horizontal rows in the triangle
 * @return     The total number of blocks in the triangle pyramid
 */
int RecursionExercises::triangle(int rows)
{

    return -1;
}
